# ColoGround-Bench dependencies
#
# PyTorch runtime is installed separately and is the ONLY frozen binary runtime:
#
#   pip install torch==2.13.0 torchvision==0.28.0 \
#     --index-url https://download.pytorch.org/whl/cu126
#
# Do not add torch here. This prevents pip from replacing the user-selected
# CUDA 12.6 PyTorch build.
#
# torchvision is installed together with torch above because it is ABI-coupled
# to the PyTorch build. All other dependencies below are intentionally flexible.
# If torchaudio already exists and has a mismatched CUDA build, run.sh repairs
# torchaudio only (with --no-deps), leaving torch untouched.

transformers>=5.17,<6
accelerate>=1.2
modelscope>=1.31
qwen-vl-utils>=0.0.8

numpy>=1.26,<3
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5

nibabel>=5.2
Pillow>=10.4
opencv-python-headless>=4.10

PyYAML>=6.0
tqdm>=4.66
einops>=0.8
timm>=1.0
sentencepiece>=0.2
safetensors>=0.4
packaging>=24.0

pytest>=8.3
