# ColoGround-Bench Python dependencies
#
# IMPORTANT: install PyTorch FIRST with the CUDA 12.6 wheel index:
#
#   pip install torch==2.13.0 torchvision==0.28.0 torchaudio==2.13.0 \
#     --index-url https://download.pytorch.org/whl/cu126
#
# Then install this file:
#
#   pip install -r requirements.txt
#
# torch / torchvision / torchaudio are intentionally NOT listed below, so pip
# will not replace the explicitly selected CUDA 12.6 builds.

transformers==5.17.0
accelerate>=1.2
modelscope>=1.31.0
qwen-vl-utils>=0.0.8

numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5

nibabel>=5.2
Pillow>=10.4
opencv-python-headless>=4.10

PyYAML>=6.0
tqdm>=4.66
einops>=0.8
timm>=1.0
sentencepiece>=0.2
safetensors>=0.4
packaging>=24.0

pytest>=8.3
